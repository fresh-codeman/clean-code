class Employee {
  constructor(name, age, salary) {
    this.name = name;
    this.age = age;
    this.salary = salary;
  }

  calculatePay() {
    // logic to calculate the pay for employee
  }

  save() {
    // save this to the db
  }

  print() {
    // show the detail of employee in a certain format
  }
}